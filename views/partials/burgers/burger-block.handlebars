<li>
	{{name}}

	<button class="change-devour btn btn-dark btn-sm" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!{{else}}EAT IT AGAIN!{{/if}}
	</button>

	<button class="delete-burger btn btn-dark btn-sm" data-id="{{this.id}}">DELETE!</button>
</li>


